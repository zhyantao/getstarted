# 枚举类型

```{code-block} c
#include <stdio.h>

int main()
{
    enum day {Monday=1, Tuesday, Wednesday};    // 初始化
    enum day today = Tuesday;                   // 变量的使用
    printf("%d\n", Wednesday);
    return 0;
}
```
